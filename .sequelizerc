// .sequelizerc  (in your project root)
const path = require("path");

module.exports = {
  config:          path.resolve(__dirname, "config", "config.js"),
  "models-path":   path.resolve(__dirname, "src", "models"),
  "migrations-path": path.resolve(__dirname, "migrations"),  // ← root folder
  "seeders-path":    path.resolve(__dirname, "seeders"),    // if you have one
};
